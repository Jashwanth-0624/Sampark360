export async function ExtractDataFromUploadedFile(file) {
  // Placeholder stub: returns empty result
  return { rows: [] }
}

export async function UploadFile(file) {
  // Placeholder stub: pretend upload succeeded
  return { ok: true, url: '' }
}

export default { ExtractDataFromUploadedFile, UploadFile }
